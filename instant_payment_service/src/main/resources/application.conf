database {
    postgres {
        connectionPool = "HikariCP"
        dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
        numThreads = 10

        properties = {
            serverName = localhost
            portNumber = 5434
            databaseName = "instant_payment_service"
            user = "postgres"
            password = "12345"
        }
    }
}